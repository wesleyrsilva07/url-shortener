# JWT secret
SECRET_JWT=your_jwt_secret

# NestJS server port
NEST_PORT=3010

# Database type used by the application (e.g., postgres, mysql)
DATABASE_TYPE=postgres

# Base URL used to build shortened links (e.g., http://localhost:3000)
DOMAIN_URL=http://localhost:3000

# Database config for local development
DATABASE_TYPE=postgres
DATABASE_PORT=5432
DATABASE_HOST=localhost
DATABASE_NAME=db_name
DATABASE_USER=root
DATABASE_PASSWORD=******